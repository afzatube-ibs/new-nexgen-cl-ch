{
  "name": "@nexgen/api-client",
  "version": "0.1.0",
  "private": true,
  "description": "Typed REST client against apps/backend's API (06_API_STANDARD.md), per ADR-0009. Hand-maintained until the backend gets an OpenAPI schema (ARCHITECTURE_REVIEW_PHASE1.md finding B-5) — a named, accepted piece of debt, per ADR-0005's own Consequences.",
  "type": "module",
  "sideEffects": false,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^4.1.11"
  }
}
