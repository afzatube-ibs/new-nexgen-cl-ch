{
  "name": "@nexgen/tokens",
  "version": "0.1.0",
  "private": true,
  "description": "neXgen Core design tokens — the single source of truth docs/frontend/DESIGN_SYSTEM.md §1 specifies. Consumed by packages/ui, apps/admin's Tailwind config, and any runtime code needing a token value directly (e.g. Chart series colors). Never redefined per consumer.",
  "type": "module",
  "sideEffects": false,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./tailwind-preset": "./tailwind-preset.js"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
